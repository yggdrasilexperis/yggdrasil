server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression for text-based assets
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1024;

    # SPA fallback: let React Router / Vite Router handle unknown paths
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache hashed static assets aggressively (Vite fingerprints filenames)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Don't cache index.html itself, so deploys are picked up immediately
    location = /index.html {
        add_header Cache-Control "no-cache";
    }
}


# server {
#     listen 443 ssl;
#     http2 on;

#     ssl_certificate     /etc/nginx/certs/nginx.crt;
#     ssl_certificate_key /etc/nginx/certs/nginx.key;

#     # Protocols — TLS 1.2+ only
#     ssl_protocols TLSv1.2 TLSv1.3;

#     # Ciphers — strong defaults, prefer server order
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;

#     # SSL session caching
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;

#     # HSTS — tell browsers to always use HTTPS (1 year)
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

#     root /usr/share/nginx/html;
#     index index.html;

#     # SPA fallback — required for React Router
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }